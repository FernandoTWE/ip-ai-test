---
const fetchIP = async () => {
  const response = await fetch("https://api.ipify.org?format=json");
  const json = await response.json();
  return json.ip;
};

const IP = await fetchIP
---

<html lang="es">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>
	</head>
	<body>
		<section class="flex items-center justify-center min-h-screen bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 text-white">
			<div class="relative container px-4 mx-auto z-10">
			  <div class="text-center max-w-3xl mx-auto bg-white bg-opacity-90 p-16 rounded-3xl shadow-2xl backdrop-filter backdrop-blur-lg">
				<span class="block mb-4 font-medium uppercase tracking-widest text-xs leading-4 text-gray-700">Test de prueba de análisis</span>
				<h2 class="mb-6 font-heading text-5xl md:text-6xl xl:text-7xl leading-tight text-gray-900">Análisis de IP</h2>
				<p class="mb-10 xl:mb-12 font-sans font-normal text-lg leading-6 text-gray-700">Esta es tu IP: <span class="font-semibold">{IP}</span></p>
				<a class="inline-block py-5 px-10 text-xl leading-6 text-white font-medium tracking-tighter font-heading bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 focus:ring-2 focus:ring-blue-600 focus:ring-opacity-50 rounded-xl shadow-xl transition transform hover:-translate-y-1" href="#">Analizar IP</a>
			  </div>
			</div>
		  </section>
		  
		  
		
	</body>
</html>
